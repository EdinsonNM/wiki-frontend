# CLAUDE.md

Sigue `AGENTS.md` como contrato principal del proyecto.

Para contexto gradual:

1. Lee `AGENTS.md`.
2. Lee `wiki/index.md`.
3. Skills y comandos de Claude Code: tras `frontend-agent-devkit setup --tool claude` quedan en `.claude/skills` y `.claude/commands`. Si trabajas en el **repo del paquete**, la fuente versionada es `tools/agents-kit/` (equivalente a `.agents/` tras `init` en proyectos consumidores).
4. Subagentes: `.claude/agents` tras ese `setup`; fuente del kit en `tools/agents-kit/agents` o `.agents/agents`.

No cargues toda la wiki salvo que el usuario pida una auditoria documental completa.

