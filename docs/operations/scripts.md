# Scripts

Actualizar desde `package.json` o herramienta real del proyecto.

| Comando | Uso | Verificado |
| --- | --- | --- |
| `pnpm dev` / equivalente | Servidor local | no |
| `pnpm lint` / equivalente | Lint | no |
| `pnpm test` / equivalente | Tests | no |
| `pnpm build` / equivalente | Build productivo | no |

## Reglas

- No documentar un comando como verificado si no fue ejecutado.
- Si un comando falla por configuracion externa, mantenerlo y documentar causa probable.
- Si cambia `package.json`, revisar esta tabla.

## Evidencia

Registrar validaciones relevantes:

```md
## YYYY-MM-DD

- Comando:
- Resultado:
- Notas:
```
