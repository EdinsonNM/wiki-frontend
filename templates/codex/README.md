# Codex Adapter

Codex usa `AGENTS.md` como guia principal del repo. Este kit mantiene skills, comandos y subagentes en `.agents/`.

Verifica con:

```bash
codex "Resume las instrucciones activas del proyecto"
```

